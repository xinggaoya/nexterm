import { ALL_COMMAND_SPECS } from "./commandSpecs";
import type { CommandSpec } from "./types";

export type CoreCommandSpec = CommandSpec;

export const CORE_COMMAND_SPECS: CoreCommandSpec[] = ALL_COMMAND_SPECS;
